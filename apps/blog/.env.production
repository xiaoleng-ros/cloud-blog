# 前台（Astro）生产环境变量 —— 在 EdgeOne 构建时被加载
# 说明：
#  - 本文件只含公开链接/站名等不敏感配置，不包含任何密钥，因此允许提交到仓库，
#    保证云端构建有默认值可用。
#  - ⚠️ 这里的 SITE_URL / PUBLIC_PAYLOAD_URL 必须是**真实线上域名**：
#    前后台一体化部署后同域（前台 / 是博客、/admin 与 /api 是 Payload 后台）。
#    如果留成占位域名，构建时 Astro 会拉不到后台数据 →
#    静态产物里就是模板默认文案（段枫 / 0 篇文章），
#    而且不会生成任何文章详情页（/posts/* 全部 404）。

# 站点根地址（canonical、RSS、OG、sitemap 等链接的基准）
SITE_URL=https://blog.iceuu.icu

# Payload 后台 API 地址（**只要域名，不带 /api**，代码会自己拼 /api/posts 等路径）：
# 前台（Astro）在构建时从后台拉取文章/随笔/项目/站点设置/导航。
PUBLIC_PAYLOAD_URL=https://blog.iceuu.icu

# Twikoo 评论服务环境 ID（留空 = 关闭评论）
PUBLIC_TWIKOO_ENV_ID=

# 网易云音乐歌单（前台音乐播放器）
PUBLIC_NETEASE_PLAYLIST_ID=8792942606
PUBLIC_MUSIC_API=https://meting.mikus.ink/api
